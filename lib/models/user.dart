class User {
  final String uid, displayName;
  User({this.uid, this.displayName});
}

class PersonalInfo {
  final String uid, name, username;
  PersonalInfo({this.uid, this.name, this.username});
}
