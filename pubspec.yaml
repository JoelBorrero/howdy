name: howdy
description: Howdy app made by TNTeam.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_spinkit: ^4.1.2+1
  firebase_core: ^1.0.1
  firebase_auth: ^1.0.1
  google_sign_in: ^5.0.0
  cloud_firestore: ^1.0.1
  provider: ^4.3.2+3
  curved_navigation_bar: ^0.3.7
  # image_picker: ^0.6.7+4
  # firebase_storage: ^3.1.5  


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true